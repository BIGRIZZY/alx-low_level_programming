#include "main.h"
/**
* sub - subtract two given integers
*
* @a: first integer
* @b: second integer
*
* Return: subtraction of the two integers
*/
int sub(int a, int b)
{
int ans;
ans = a - b;
return (ans);
}
