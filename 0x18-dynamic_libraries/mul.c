#include "main.h"
/**
* mul - multiplies two given integers
*
* @a: first integer
* @b: second integer
*
* Return: sum of the two integers
*/
int mul(int a, int b)
{
int ans;
ans = a * b;
return (ans);
}
